'use strict';

let fname = "Lalit";
const age = "22";
var havas = "Too high";
let relationship = "single";

console.log(fname + " is " + age + " years old whose hawas " + havas + " and relationship statis is " + relationship);